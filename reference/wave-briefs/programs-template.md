# Section brief — `programs` (Xterra wave X2: bikes, ellipticals, climber)

Read `$S/briefs-x2/COMMON.md` first. Your section facet is `section: programs`.

Yours: every program chapter — preset programs by name with their profile
tables (rebuild them; many are pictures — read the OCR supplement and the render
where a segment figure matters), manual mode, user / custom programs, heart-rate
/ target programs (each its own card), watt programs where a book has them, the
Heart Rate Exertion / zone page (extend the X1 card if the page is the same
with no line-specific figure — set `product_line: '*'`), the climber's programs.
Books with no programs (the MB indoor cycles, AIR bikes, FB150/FB350?) — prove
the absence twice and card it. Then act as second reader for `console`: report
(do not card) anything on the console pages that looks wrong.

Not yours: how the console reads a pulse → console; program error messages →
errors.

A program printed identically in several books is one card with `applies_to`
listing them; a profile whose figures differ is its own card with the
distinguishing value in the title.
